snowflake_profile:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "<account>"
      user: "<user>"
      password: "<password>"
      role: "SYSADMIN"
      database: "ANALYTICS"
      warehouse: "TRANSFORMING"
      schema: "DBT_STEVEN"
      threads: 4
      client_session_keep_alive: False
